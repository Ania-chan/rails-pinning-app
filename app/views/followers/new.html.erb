<h1>Find People to Follow!</h1>
<div class="row">
  <div class="col-md-6">
  <% @users.each do |user_to_follow| %>
    <%= render partial: 'form', locals: { user_to_follow: user_to_follow } %>
    <br>
  <% end %>
  </div>
</div>

<hr/>

<%= link_to 'Back to Followers List', followers_path %>
